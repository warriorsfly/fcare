<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="listener" type="com.wxsoft.fcare.ui.details.assistant.AssistantExaminationViewModel"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <ImageView
                android:id="@+id/icon"
                goneUnless="@{listener.lisRecords.size==0||listener.selectedType.equals(`3`)}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                android:src="@{listener.selectedType.equals(`3`)?@drawable/ic_big_add:@drawable/ic_data_none}"
                android:onClick="@{()-> listener.addJGDB()}"
                android:layout_margin="50dp"
                android:layout_width="80dp"
                android:layout_height="80dp"/>

        <TextView
                goneUnless="@{!(listener.selectedType.equals(`3`)||listener.lisRecords.size!=0)}"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/icon"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="10dp"
                android:text="暂无数据"
                android:textSize="15dp"
                android:textColor="#131D35"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>